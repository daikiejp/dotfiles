# ==============================================================================
# Zsh local — THIS MACHINE ONLY
# DaikieJP - 2026
#
# chezmoi creates this file once and then never touches it again (the
# `create_` prefix). It is not versioned: keys, client paths and personal
# aliases that must not end up in a public repo go here.
# ==============================================================================

# Examples:
# alias deploy='~/.config/scripts/deploy.sh'
# export SOME_API_KEY="..."
# path=("$HOME/some/odd/place/bin" $path)
