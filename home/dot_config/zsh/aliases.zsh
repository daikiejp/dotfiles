# ==============================================================================
# Zsh Aliases
# DaikieJP - 2026
#
# Only aliases that make sense on every system. Personal or client-specific
# ones live in ~/.config/zsh/local.zsh, which is never versioned.
#
# If an OS-specific alias is ever needed, rename this file to aliases.zsh.tmpl
# and wrap it:
#   {{ if eq .chezmoi.os "darwin" }} ... {{ end }}
# ==============================================================================

# ==============================================================================
# Neovim
# ==============================================================================

alias n="nvim"
